# CSE468-Project02
1. Setup packet filter firewall (iptables) to allow and block network traffic
2. Set up Network Address Translation (NAT) service
3. Use basic networking and diagnostic tools such as ifconfig, ip, route, netstat, ping, traceroute, and tcpdump.
4. Use iptables to regulate network traffic and enable or disable services such as Web, FTP and SSH based on provided traffic policies
